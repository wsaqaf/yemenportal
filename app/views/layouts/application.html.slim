doctype html
html lang="ar" dir="rtl"
  head
    meta charset="utf-8"
    meta name="viewport" content="width=device-width, initial-scale=1.0"

    title == content_for?(:title) ? yield(:title) : "Untitled"

    = stylesheet_link_tag "application"
    = javascript_include_tag "application", "data-turbolinks-track" => true
    = csrf_meta_tag

  body
    .top-bar
      .top-bar-title.float-right
        strong = link_to I18n.t('site_name'), home_page_index_path
      span.top-bar-title.float-right data-responsive-toggle="responsive-menu"
        button.menu-icon.dark data-toggle="" type="button"
      #responsive-menu
        .top-bar-right
          ul.dropdown.menu data-dropdown-menu=""
            li
              = link_to I18n.t('menu.sources'), sources_path
            li
              = link_to I18n.t('menu.categories'), categories_path
            li
              = link_to I18n.t('menu.news.name'), '#'
              ul.menu.vertical
                li
                  = link_to I18n.t('menu.news.new'), '#'
                li
                  = link_to I18n.t('menu.news.rejected'), '#'

    == yield
